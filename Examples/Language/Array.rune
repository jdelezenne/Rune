func sum(values: Array<Int>) -> Int {
    var total = 0
    for value in values {
        total = total + value
    }

    return total
}

func main() -> Int {
    let numbers: Array<Int> = [1, 2, 3, 4, 5]
    if sum(numbers) != 15 {
        return 1
    }

    return 0
}
