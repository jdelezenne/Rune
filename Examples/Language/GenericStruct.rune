struct Box<T> {
    var value: T
}

func main() -> Int {
    let numbers = Box<Int>(value: 21)
    let doubled = Box<Int>(value: numbers.value * 2)
    if doubled.value != 42 {
        return 1
    }
    return 0
}
