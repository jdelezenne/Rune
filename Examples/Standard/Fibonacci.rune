import Standard.*

func fibonacci(n: Int) -> Int {
    if n <= 1 {
        return n
    }
    return fibonacci(n - 1) + fibonacci(n - 2)
}

func main() -> Int {
    for i in 0..10 {
        Console.printLine("fib(${i}) = ${fibonacci(i)}")
    }
    return 0
}
